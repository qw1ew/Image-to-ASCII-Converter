# Build directories
build/
bin/
out/
x64/
x86/
debug/
release/

# CMake artifacts
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
*.sln
*.vcxproj
*.vcxproj.filters
*.vcxproj.user

# Executables
*.exe
*.dll
*.lib
*.pdb
*.ilk
*.exp
*.obj

# Temp files
temp_*.zip
*.tmp
*.log
.vs/
.vscode/

# Dependencies (if we want to exclude big downloaded libs, but for this self-contained repo 
# we might want to keep include/imgui etc if we want it to be "clone and build". 
# However, usually dependencies are ignored or submoduled.
# Given I wrote a setup script, maybe we can ignore them?
# The user asked to "prepare for github".
# If I ignore `include/imgui`, `include/glfw`, `include/libwebp`, the user has to run `setup_gui.ps1` and `setup_webp.ps1`.
# This is cleaner.
include/imgui/
include/glfw/
include/libwebp/

# Keep the scripts though!

# User files
*.jpg
*.png
*.webp
ascii_art.txt
